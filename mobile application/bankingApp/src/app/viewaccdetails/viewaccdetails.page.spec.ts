import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ViewaccdetailsPage } from './viewaccdetails.page';

describe('ViewaccdetailsPage', () => {
  let component: ViewaccdetailsPage;
  let fixture: ComponentFixture<ViewaccdetailsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ViewaccdetailsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
